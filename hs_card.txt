  font-size: 0.84rem;
  font-weight: 800;
  line-height: 1.2;
}
.hs-group-subtitle {
  display: block;
  margin-top: 4px;
  font-size: 0.72rem;
  font-weight: 600;
  line-height: 1.35;
  letter-spacing: 0.01em;
  text-transform: none;
  opacity: 0.82;
}
.hs-group-blue   { background: var(--blue-light);  color: var(--blue-dark); }
.hs-group-green  { background: var(--green-bg);    color: var(--green-dark); }
.hs-group-orange { background: var(--orange-bg);   color: var(--orange); }
.hs-group-purple { background: var(--purple-bg);   color: var(--purple); }

/* Cards list */
.hs-cards {
  padding: 12px;
  display: grid;
  grid-template-columns: 1fr;
  gap: 12px;
}

/* Card */
.hs-card {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 15px;
  border-radius: var(--radius);
  background: rgba(255,255,255,0.8);
  border: 1px solid rgba(0,0,0,0.06);
  text-decoration: none;
  box-shadow: 0 1px 0 rgba(255,255,255,0.04);
}
.hs-card-icon {
  width: 42px;
  height: 42px;
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.hs-card-icon svg { width: 18px; height: 18px; }
.hs-accent-blue   .hs-card-icon { background: var(--blue-light);  color: var(--blue); }
.hs-accent-green  .hs-card-icon { background: var(--green-bg);    color: var(--green-dark); }
.hs-accent-orange .hs-card-icon { background: var(--orange-bg);   color: var(--orange); }
.hs-accent-purple .hs-card-icon { background: var(--purple-bg);   color: var(--purple); }

/* Card body */
.hs-card-body { flex: 1; min-width: 0; }
.hs-card-name {
  display: block;
  font-size: 0.94rem;
  font-weight: 700;
  color: var(--text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.hs-card-desc {
  display: block;
  font-size: 0.79rem;
  color: #4b5d70;
  line-height: 1.35;
  margin-top: 2px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Arrow */
.hs-card-arrow {
  font-size: 1rem;
  color: var(--text-3);
  opacity: 0;
  transform: translateX(-4px);
  transition: opacity .15s, transform .15s;
  flex-shrink: 0;
}

/* Sidebar home button */
.nav-home-btn {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  font-size: 0.82rem;
  font-weight: 600;
  color: var(--text-2);
  background: transparent;
  border: none;
  cursor: pointer;
  border-radius: var(--radius-sm);
  width: 100%;
  text-align: left;
  transition: background .15s, color .15s;
  font-family: inherit;
}
.nav-home-btn:hover, .nav-home-btn.active {
  background: var(--blue-light);
  color: var(--blue-dark);
}
.nav-home-btn svg { width: 16px; height: 16px; flex-shrink: 0; }

/* Responsive */
@media (max-width: 960px) {
  .hs-hero { grid-template-columns: 1fr; }
  .hs-overview { grid-template-columns: 1fr; }
  .hs-modules { grid-template-columns: 1fr; }
}
@media (max-width: 600px) {
  #home-screen { padding: 12px 12px 32px; }
  .hs-hero-main { gap: 12px; }
  .hs-group-header { padding: 12px 14px; }
  .hs-group-subtitle { font-size: 0.7rem; }
  .hs-overview { grid-template-columns: 1fr; }
  .hs-overview-card { padding: 14px; }
  .hs-overview-value { font-size: 1.3rem; }
  .hs-brasao-wrap { width: 52px; height: 52px; border-radius: 14px; }
  .hs-brasao { max-width: 78%; max-height: 78%; }
  .hs-title { font-size: 1.12rem; }
  .hs-subtitle { font-size: 0.9rem; }
  .hs-hero-actions { flex-direction: column; }
  .hs-clock { font-size: 1.5rem; }
  .hs-card { padding: 12px; }
  .hs-card-name { font-size: 0.86rem; }
  .hs-card-desc { font-size: 0.74rem; white-space: normal; }
}
@media (hover: none) {
  .hs-card:hover { transform: none; }
}
